
#include "exo1.h"
#include <stdio.h>
#include "syracuse.h"



int main()
{

    // Tableau qui stocke les valeurs de la fonction de syracuse
    int *syracuse;

    syracuse = (int *)malloc((SIZE + 1) * sizeof(int));

    free(syracuse);
    return 0;
}


