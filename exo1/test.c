
#include <stdio.h>
#include "cache.h"
/*
int main(int argc, char *argv[]) {


    cache_t cache ;

    cache_init(&cache);


    printf("%d\n", cache_get(&cache, 4));

    cache_set(&cache,4,9);

    printf("%d\n", cache_get(&cache, 4));

    cache_del(&cache,4);
    cache_del(&cache,4);

    printf("%d\n", cache_get(&cache, 4));

    cache_destroy(&cache);

    return 0;
}

 */